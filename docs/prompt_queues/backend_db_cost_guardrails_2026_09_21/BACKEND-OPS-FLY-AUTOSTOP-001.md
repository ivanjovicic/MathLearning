# BACKEND-OPS-FLY-AUTOSTOP-001 â€” Auto-stop the idle Fly API machine before real production traffic\n\nPrompt contract: v2\nRepository: `ivanjovicic/MathLearning`\nPrompt ID: `BACKEND-OPS-FLY-AUTOSTOP-001`\nQueue: `docs/prompt_queues/backend_db_cost_guardrails_2026_09_21.md`\nPriority: P0 cost/reliability\nStatus: Ready\nRun lane: known-fix\nToken budget: low\nTimebox: 15 minutes\nDelivery target: main\n\nPrompt admission: v3\nProblem evidence: The current Fly service has no explicit auto-stop/auto-start policy and can keep an idle machine alive.\nDeduplication check: `BACKEND-OPS-DB-IDLE-BUDGET-001` owns worker polling; this prompt owns only Fly machine lifecycle configuration.\nPriority rationale: P0 because idle machine cost is avoidable before real production traffic.\nDependencies/collisions: Preserve current ports, volume, DataProtection path and DB-free health check; do not touch runtime workers.\nOwner boundary: `fly.toml`, deployment documentation and an existing static config validation owner if present.\nQueue placement: `backend_db_cost_guardrails_2026_09_21.md`.\n\nTask: Configure explicit Fly pre-production auto-stop/auto-start with zero minimum warm machines and document the operator behavior.\nSource of truth: `fly.toml`, `FLY_DEPLOYMENT_GUIDE.md` and current Fly service/health-check configuration.\nInterpretation before work: Cold-start latency is acceptable pre-production; no live savings claim is made without Fly observation.\nAmbiguity rule: Preserve existing service topology and stop for any request to change runtime worker correctness or secrets.\nRisk/ownership model: Configuration may stop a machine but must not remove durable volumes or alter health semantics.\nFailure-mode matrix: Valid TOML; unavailable Fly CLI; cold-start; auto-start; auto-stop; preserved health/volume/ports.\nExecution packet: Read the current Fly config/docs, add only the lifecycle keys and operator notes, then run static validation.\nDocumentation impact: Update `FLY_DEPLOYMENT_GUIDE.md` and record unavailable live Fly proof honestly.\nAcceptance criteria: Config has stop/start/zero-minimum settings, existing service topology is unchanged, and cold-start/operator behavior is documented.\nProof required: TOML/static assertions, documentation health and any available `fly config validate`; no deployment claim without live observation.\nTest-first contract: Static config regression must fail before the keys are added and pass afterward, with a counterexample proving ports/volume/health are preserved.\nValidation: Static configuration test, documentation/prompt validators and `fly config validate` when the CLI is available.\nCompletion gate: Focused static proof, evidence, changed-only validation and delivery to `main`; runtime savings remain an operator follow-up.\nStop conditions: Runtime worker edits, secret changes, second machine/process group, missing proof or a 15-minute budget breach.\nEvidence: `.ai/runs/2026-09-21-BACKEND-OPS-FLY-AUTOSTOP-001-evidence.md`.\n\n## Current-main evidence\n\n- `fly.toml` exposes the API through `[[services]]` but does not set `auto_stop_machines`, `auto_start_machines` or `min_machines_running`.\n- Current Fly configuration defaults auto-stop to off when it is not configured.\n- The app currently has one Fly machine and one attached local volume for DataProtection keys.\n- The Fly health check uses `/api/health/`, which is intentionally DB-free.\n- The project is not yet serving real production traffic, so cold-start latency is preferable to keeping Fly + Postgres continuously active.\n\n## Required work\n\n1. Verify the current Fly configuration reference before editing.\n2. Configure the existing service for:\n   - `auto_stop_machines = "stop"`;\n   - `auto_start_machines = true`;\n   - `min_machines_running = 0`.\n3. Preserve the existing internal port, HTTP/TLS ports, volume mount, DataProtection path and DB-free health check.\n4. Do not add a second machine/process group or remove the volume.\n5. Document expected pre-production behavior:\n   - first request after idle can cold-start the API;\n   - Fly automatically starts the machine;\n   - idle machine can stop;\n   - background hosted services stop with the machine and therefore cannot keep waking Postgres while the VM is stopped.\n6. Add/extend a static Fly config validation if the repo already has one; otherwise use `fly config validate` when available and record inability honestly.\n7. Do not modify runtime worker correctness in this prompt.\n\nOwned paths:\n- `fly.toml`\n- deployment/operations documentation\n- static config validation test only if an existing owner exists\n\nAvoid paths:\n- application DB worker implementation\n- Neon secrets/connection strings\n- machine count/volume deletion\n- health endpoint semantics\n\n## Proof required\n\n- TOML/config validation;\n- existing health endpoint tests unchanged;\n- Release build if runtime source is touched (it should not be);\n- operator follow-up command documented for deploy/status verification.\n\n## Deployment smoke after merge\n\n```powershell\nfly config validate -a mathlearning-api\nfly deploy -a mathlearning-api\nfly status -a mathlearning-api\ncurl.exe --max-time 20 https://mathlearning-api.fly.dev/api/health/\n```\n\nDo not claim deployed savings until the machine is observed stopping after idle and auto-starting on the next request.\n